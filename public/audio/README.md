# Background music

Drop a licensed audio file here named `theme-song.mp3` and the site's music
toggle (Y2K / MySpace themes) will automatically play it instead of the
built-in synthesized melody. No code changes needed.

If no file is present, the toggle falls back to an original, synthesized
piano melody generated in the browser (see `src/components/AudioToggle.tsx`).
